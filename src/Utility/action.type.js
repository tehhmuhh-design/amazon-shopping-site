export const Type = {
  ADD_TO_BASKET: "ADD_TO_BASKET", // Action type for adding an item to the basket (cart)

  REMOVE_FROM_BASKET: "REMOVE_FROM_BASKET", // Action type for removing an item from the basket (cart)

  SET_USER: "SET_USER", // Action type for setting the user information (login/logout)

  SET_AUTH_LOADING: "SET_AUTH_LOADING", // Action type for tracking whether Firebase has resolved the session yet

  EMPTY_BASKET: "EMPTY_BASKET", // Action type for emptying the basket (clearing all items)
};